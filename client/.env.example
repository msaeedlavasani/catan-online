# URL of the Socket.io server.
# For production, set this before `npm run build` to the public backend URL.
# Leave it empty only when frontend and backend share the same origin.
VITE_SERVER_URL=http://localhost:4000
